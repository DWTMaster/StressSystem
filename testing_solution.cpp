#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
#define STRESS
#ifdef STRESS
    freopen(argv[1], "r", stdin);
    freopen(argv[2], "w", stdout);
#endif
    int n;
    cin >> n;
    cout << n * n << '\n';
}
